import React from "react";
import Profile from "../components/auth/UserProfile"; // Adjust the path if needed

const ProfilePage: React.FC = () => {
  return <Profile />;
};

export default ProfilePage;
