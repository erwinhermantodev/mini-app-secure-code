import React from "react";
import RegisterForm from "../components/auth/RegisterForm"; // Adjust the path if needed

const RegisterPage: React.FC = () => {
  return <RegisterForm />;
};

export default RegisterPage;
